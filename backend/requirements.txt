fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# AWS
boto3>=1.34
langchain-aws>=0.2

# LangChain core
langchain>=0.3
langchain-community>=0.3

# Vector store + embeddings
qdrant-client[fastembed]>=1.12

# Document parsing & chunking
docling>=2.15

# NOTE: torch is installed CPU-only via the setup script / Dockerfile
# before this file is processed, to avoid the 3 GB CUDA build.
# Pin to 4.x — transformers 5.x broke AutoProcessor lazy-import used by docling.
transformers>=4.40,<5.0

# Reranking
sentence-transformers>=3.0

# Cognito JWT verification
python-jose[cryptography]>=3.3
requests>=2.31
